server:
  port: 9090
spring:
  application:
    name: todo-management-projects
  datasource:
    url: jdbc:mysql://localhost:3306/todomanagementprojects?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQLDialect
